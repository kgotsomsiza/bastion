/* ============================================================
   YOUR TEST TASKS

   1. Replace  TYPE YOUR PROMPT HERE  below with your own prompt.
   2. Run:
        powershell -ExecutionPolicy Bypass -File scripts/test_container.ps1 -Tasks mytasks.json

   To test SEVERAL prompts at once, see the "EXTRA TASKS" note below.
   ============================================================ */
[
  {
    "task_id": "t1",
    "prompt": "How many countries are there on earth"
  }

  /* EXTRA TASKS (optional - currently switched off):
     To turn these on, delete the comment-OPEN marker (slash-star) on the
     line directly above, and the comment-CLOSE marker (star-slash) on the
     line directly below. Keep a comma after every task except the last one.

  ,
  {
    "task_id": "t2",
    "prompt": "TYPE YOUR SECOND PROMPT HERE"
  },
  {
    "task_id": "t3",
    "prompt": "TYPE YOUR THIRD PROMPT HERE"
  }

  */
]
